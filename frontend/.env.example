# Local dev: leave unset to use /api with the Vite dev proxy (vite.config.ts → localhost:8000).
# Production build: set in Railway or rely on .env.production in this folder.
# VITE_API_URL=https://dmrb.up.railway.app/api
